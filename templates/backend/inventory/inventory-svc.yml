{{- if .Values.backend.enabled }}
  {{- if .Values.backend.services.inventory.enabled }}
apiVersion: v1
kind: Service
metadata:
  annotations:
    {{- range $key, $value := .Values.backend.services.inventory.service.annotations }}
      {{- if not (eq $key "nil") }}
    {{ $key }}: {{ $value }}
      {{- end }}
    {{- end }}
  labels:
{{ include "spaceone.labels" . | indent 4 }}
    helm.stargate.spaceone.dev/service: inventory
  name: inventory
  namespace: {{ .Release.Namespace }}
spec:
    {{- with .Values.backend.services.inventory }}
  ports:
{{ toYaml .service.ports | indent 4 }}
  selector:
    helm.stargate.spaceone.dev/service: inventory
  sessionAffinity: None
  type: {{ .service.type }}
    {{- end }}
  {{- end }}
{{- end }}